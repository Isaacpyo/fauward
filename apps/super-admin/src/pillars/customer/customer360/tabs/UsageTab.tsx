import type { Customer360 } from "../api";
export default function UsageTab({ data }: { data: Customer360 }) {
  const apiUsage = data.usage.apiUsage;
  return <div className="grid gap-4 lg:grid-cols-3"><section className="rounded-lg border border-[var(--color-border)] bg-white p-4"><h2 className="text-sm font-semibold">Shipments by status</h2>{data.usage.shipmentStatusBreakdown.map((row) => <p key={row.status} className="mt-2 flex justify-between text-sm"><span>{row.status}</span><span>{row._count.id}</span></p>)}</section><section className="rounded-lg border border-[var(--color-border)] bg-white p-4"><h2 className="text-sm font-semibold">API Usage</h2><p className="mt-3 text-2xl font-bold">{apiUsage?.totalRequestsThisMonth ?? 0}</p><p className="text-xs text-[var(--color-text-muted)]">{apiUsage?.averagePerDay ?? 0} avg/day across {apiUsage?.keys.length ?? 0} keys</p>{apiUsage?.keys.slice(0, 4).map((key) => <p key={key.id} className="mt-2 flex justify-between text-xs"><span>{key.name ?? key.id}</span><span>{key.monthlyRequestCount}</span></p>)}</section><section className="rounded-lg border border-[var(--color-border)] bg-white p-4"><h2 className="text-sm font-semibold">Regional breakdown</h2><p className="mt-3 text-sm text-[var(--color-text-muted)]">{data.tenant.shipments.length} recent shipments sampled.</p></section></div>;
}
