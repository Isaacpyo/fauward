import { MonoCell } from "@fauward/internal-ui";
import type { Customer360 } from "../api";
export default function ConfigTab({ data }: { data: Customer360 }) {
  return <div className="space-y-4"><section className="rounded-lg border border-[var(--color-border)] bg-white p-4"><h2 className="text-sm font-semibold">Feature Flags</h2><div className="mt-3 divide-y divide-[var(--color-border)]">{(data.featureFlagOverrides ?? []).slice(0, 10).map((flag) => <div key={flag.id} className="flex justify-between py-2 text-sm"><span>{flag.flagKey}</span><span className="font-mono text-xs">{JSON.stringify(flag.value)}</span></div>)}{(data.featureFlagOverrides ?? []).length === 0 ? <p className="text-sm text-[var(--color-text-muted)]">No tenant overrides recorded.</p> : null}</div></section><section className="rounded-lg border border-[var(--color-border)] bg-white p-4"><h2 className="text-sm font-semibold">Config JSON</h2><pre className="mt-3 overflow-auto rounded bg-[var(--color-surface-50)] p-3 text-xs"><MonoCell value={JSON.stringify(data.tenant, null, 2)} /></pre></section></div>;
}
