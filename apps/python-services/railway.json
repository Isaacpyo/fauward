{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "apps/python-services/Dockerfile"
  },
  "deploy": {
    "healthcheckPath": "/health",
    "startCommand": "sh -c \"celery -A celery_app worker --loglevel=info -Q pdf,routes,analytics,ocr,notifications,pricing,customs,ml --pool=solo & celery -A celery_app beat --loglevel=info & uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}\""
  }
}
